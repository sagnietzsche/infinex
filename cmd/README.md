## Commands to be supported

- run 
    - (Flags)
    --model -> define the model gguf format 
    --prompt -> simple text quoted string that needs to be passed as prompt
    --max-tokens -> the maximum number of tokens that we can allow it to give and run 